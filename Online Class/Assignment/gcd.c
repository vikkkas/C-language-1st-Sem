#include<stdio.h>
int gcd();
int main()
{
    int a,b;
    printf("Enter any two number whose GCD is to be found:");
    scan("%d%d",&a,&b);

     
    return 0;
}
int gcd(){
    for(i=1; i<=a && i<=b;i++)
    {
        if
    }
}