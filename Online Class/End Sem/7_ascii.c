#include <stdio.h>
int main()
{
    char ascii;
    printf("enter any character whose ASCII code is to be obtained:");
    scanf("%c", &ascii);
    printf("The ASCII value for the entered character is : %d \n", ascii);
    return 0;
}